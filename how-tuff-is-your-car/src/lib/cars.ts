// Backward-compatible exports for older components. New code should import from
// @/lib/catalog so remote and classic entries are included.
export { curatedCars as cars, getCarLabel, getOverallScore, getVerdict, getVerdictCopy, ratingKeys, ratingLabels } from "@/lib/catalog";
